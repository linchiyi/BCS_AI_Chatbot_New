# Streamlit Secrets 範本
# 
# 本地測試：將此檔案複製為 secrets.toml 並填入實際值
# Streamlit Cloud：在網頁介面的 Settings > Secrets 中貼上此內容
#
# ⚠️ 注意：secrets.toml 已加入 .gitignore，不會被上傳到 Git

# ========================================
# 方法 1：使用 OAuth 2.0（推薦，個人 Drive）
# ========================================

# Google Drive 資料夾 ID
DRIVE_FOLDER_ID = "16HRRkutsZcscFkk4Q7XgJPEjbz3nurod"

# OAuth 2.0 Token
# 執行以下步驟取得：
# 1. 本地執行：python google_drive_utils.py（完成授權）
# 2. 轉換 token：python token_to_secrets.py
# 3. 複製生成的內容到這裡或 Streamlit Cloud Secrets
[oauth_token]
token = "your-access-token-here"
refresh_token = "your-refresh-token-here"
token_uri = "https://oauth2.googleapis.com/token"
client_id = "your-client-id.apps.googleusercontent.com"
client_secret = "your-client-secret"
scopes = [
  "https://www.googleapis.com/auth/drive.file",
]

# ========================================
# 方法 2：使用 Service Account（適合企業 Drive）
# ========================================

# 如果使用 Service Account，請註解掉上面的 [oauth_token] 並使用以下格式：
# [gcp_service_account]
# type = "service_account"
# project_id = "your-project-id"
# private_key_id = "your-private-key-id"
# private_key = "-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
# client_email = "your-service-account@your-project.iam.gserviceaccount.com"
# client_id = "your-client-id"
# auth_uri = "https://accounts.google.com/o/oauth2/auth"
# token_uri = "https://oauth2.googleapis.com/token"
# auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
# client_x509_cert_url = "your-cert-url"
# universe_domain = "googleapis.com"
